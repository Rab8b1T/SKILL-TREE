# Contest System Architecture Diagram

## 🏗️ System Overview

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER'S BROWSER                              │
│                                                                     │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │                   Contest Web App                            │  │
│  │                                                              │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌───────────┐  │  │
│  │  │  HTML5   │  │   CSS3   │  │ Vanilla  │  │ LocalStor │  │  │
│  │  │  Markup  │  │  Styles  │  │    JS    │  │   -age    │  │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └───────────┘  │  │
│  │       │              │              │              │         │  │
│  │       └──────────────┴──────────────┴──────────────┘         │  │
│  │                           │                                   │  │
│  └───────────────────────────┼───────────────────────────────────┘  │
│                              │                                       │
└──────────────────────────────┼───────────────────────────────────────┘
                               │
                               │ HTTPS/REST API
                               │ (fetch requests)
                               │
┌──────────────────────────────▼───────────────────────────────────────┐
│                      BACKEND SERVER                                  │
│                  (Python Flask + Vercel)                             │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │                  Flask Application                          │    │
│  │                                                             │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │    │
│  │  │  Routes  │  │  CORS    │  │  Error   │  │  Health  │  │    │
│  │  │ /api/*   │  │ Handling │  │ Handler  │  │  Check   │  │    │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │    │
│  │       │              │              │              │       │    │
│  │       └──────────────┴──────────────┴──────────────┘       │    │
│  │                           │                                 │    │
│  └───────────────────────────┼─────────────────────────────────┘    │
│                              │                                       │
└──────────────────────────────┼───────────────────────────────────────┘
                               │
                               │ PyMongo Driver
                               │ (MongoDB connection)
                               │
┌──────────────────────────────▼───────────────────────────────────────┐
│                      MONGODB ATLAS                                   │
│                    (Cloud Database)                                  │
│                                                                      │
│  ┌────────────────────────────────────────────────────────────┐    │
│  │               Database: skilltree                           │    │
│  │                                                             │    │
│  │  ┌───────────────────┐         ┌────────────────────┐     │    │
│  │  │  contest_data     │         │  contests          │     │    │
│  │  │                   │         │                    │     │    │
│  │  │  - user           │         │  - user            │     │    │
│  │  │  - pastContests   │         │  - contestId       │     │    │
│  │  │  - streak         │         │  - problems        │     │    │
│  │  │  - settings       │         │  - scores          │     │    │
│  │  │  - lastSyncTime   │         │  - date            │     │    │
│  │  │                   │         │                    │     │    │
│  │  │  Index: user      │         │  Index: user+id    │     │    │
│  │  └───────────────────┘         └────────────────────┘     │    │
│  │                                                             │    │
│  └─────────────────────────────────────────────────────────────┘    │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow Diagram

```
USER ACTION                 FRONTEND                BACKEND              DATABASE
────────────                ────────                ───────              ────────

  Start
  Contest
    │
    │ 1. User clicks
    │    "Start Contest"
    ├──────────────────>
    │                   Generate
    │                   contest data
    │                        │
    │                        │ 2. Save initial
    │                        │    snapshot
    │                        ├──────────────────>
    │                        │                   POST /contest/data
    │                        │                        │
    │                        │                        │ 3. Insert
    │                        │                        │    document
    │                        │                        ├────────────>
    │                        │                        │              MongoDB
    │                        │                        │              Write
    │                        │                        │<────────────
    │                        │                        │ 4. Confirm
    │                        │<──────────────────
    │                        │ 5. Success response
    │<──────────────────
    │ 6. Show contest
    │    & start timer
    │
    │
  Solve
  Problem
    │
    │ 7. Solve on CF
    │    Submit solution
    │
    │ 8. Click "Refresh"
    ├──────────────────>
    │                   Fetch CF API
    │                   (submissions)
    │                        │
    │                        │ 9. Update local
    │                        │    state
    │                        │
    │                        │ 10. Sync to DB
    │                        ├──────────────────>
    │                        │                   POST /contest/data
    │                        │                        │
    │                        │                        │ 11. Upsert
    │                        │                        │     document
    │                        │                        ├────────────>
    │                        │                        │
    │                        │                        │<────────────
    │                        │<──────────────────
    │<──────────────────
    │ 12. Update UI
    │     (solved count,
    │      score, etc.)
    │
    │
  Switch
  Device
    │
    │ 13. Open on
    │     new device
    ├──────────────────>
    │                   Load user
    │                   profile
    │                        │
    │                        │ 14. Fetch data
    │                        │     from DB
    │                        ├──────────────────>
    │                        │                   GET /contest/data
    │                        │                        │
    │                        │                        │ 15. Query
    │                        │                        │     by user
    │                        │                        ├────────────>
    │                        │                        │
    │                        │                        │<────────────
    │                        │                        │ 16. Return
    │                        │<──────────────────     data
    │                        │ 17. Data received
    │<──────────────────
    │ 18. Restore state
    │     (contests,
    │      streak, etc.)
```

## 🌐 Network Architecture

```
┌────────────┐
│   User     │
│  Browser   │
└─────┬──────┘
      │
      │ HTTPS (443)
      │ DNS: your-domain.vercel.app
      │
┌─────▼──────────────────────────────┐
│     Vercel Edge Network            │
│     (CDN + Load Balancing)         │
└─────┬──────────────────────────────┘
      │
      ├─────> Static Files (HTML/CSS/JS)
      │       Served from CDN
      │       (cached globally)
      │
      └─────> API Requests (/api/*)
              │
              │ Routed to serverless function
              │
      ┌───────▼──────────────────┐
      │  Python Flask Function   │
      │  (Serverless)            │
      │                          │
      │  - Auto-scaling          │
      │  - Per-request billing   │
      │  - 10s timeout           │
      └───────┬──────────────────┘
              │
              │ MongoDB Connection
              │ (SSL encrypted)
              │
      ┌───────▼──────────────────┐
      │   MongoDB Atlas          │
      │   (M0 Free Tier)         │
      │                          │
      │  - 512 MB storage        │
      │  - Replicated (3 nodes)  │
      │  - Auto-backups          │
      └──────────────────────────┘
```

## 🔐 Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│                     SECURITY LAYERS                         │
└─────────────────────────────────────────────────────────────┘

Layer 1: Transport Security
┌──────────────────────────────────┐
│  • HTTPS/TLS 1.3                 │
│  • SSL Certificates (Vercel)     │
│  • Secure WebSocket (wss://)     │
└──────────────────────────────────┘
              │
              ▼
Layer 2: Application Security
┌──────────────────────────────────┐
│  • CORS Configuration            │
│  • Input Validation              │
│  • Error Sanitization            │
│  • Rate Limiting (CF API)        │
└──────────────────────────────────┘
              │
              ▼
Layer 3: Authentication
┌──────────────────────────────────┐
│  • MongoDB User Auth             │
│  • Connection String Security    │
│  • Environment Variables         │
│  • No Hardcoded Credentials      │
└──────────────────────────────────┘
              │
              ▼
Layer 4: Database Security
┌──────────────────────────────────┐
│  • IP Whitelist                  │
│  • User-level Permissions        │
│  • Encrypted at Rest             │
│  • Encrypted in Transit          │
│  • Audit Logs                    │
└──────────────────────────────────┘
              │
              ▼
Layer 5: Data Protection
┌──────────────────────────────────┐
│  • User Data Isolation           │
│  • LocalStorage Backup           │
│  • Export Functionality          │
│  • No PII Collection             │
└──────────────────────────────────┘
```

## 📦 Deployment Pipeline

```
┌─────────────┐
│  Developer  │
│    Code     │
└──────┬──────┘
       │
       │ git push
       │
┌──────▼───────────────────────────┐
│   Git Repository                 │
│   (GitHub/GitLab/Bitbucket)      │
└──────┬───────────────────────────┘
       │
       │ Webhook trigger
       │
┌──────▼───────────────────────────┐
│   Vercel Build System            │
│                                  │
│  1. Detect changes               │
│  2. Install dependencies         │
│  3. Build Python function        │
│  4. Optimize static files        │
│  5. Run tests (if configured)    │
└──────┬───────────────────────────┘
       │
       │ Deploy
       │
┌──────▼───────────────────────────┐
│   Vercel Edge Network            │
│                                  │
│  • Global CDN                    │
│  • Automatic HTTPS               │
│  • Serverless Functions          │
│  • Environment Variables         │
└──────┬───────────────────────────┘
       │
       │ Connect
       │
┌──────▼───────────────────────────┐
│   MongoDB Atlas                  │
│                                  │
│  • Cloud Database                │
│  • Always Available              │
│  • Auto-Scaling                  │
└──────────────────────────────────┘
       │
       ▼
   🎉 LIVE!
```

## 🔄 Sync Strategy

```
                     DUAL PERSISTENCE STRATEGY
                     ========================

┌────────────────────────────────────────────────────────────┐
│                    Browser State                           │
│                   (in-memory data)                         │
└───────────┬────────────────────────┬───────────────────────┘
            │                        │
            │ On change              │ On change
            │ (immediate)            │ (immediate)
            │                        │
            ▼                        ▼
┌───────────────────────┐  ┌────────────────────────────────┐
│   LocalStorage        │  │    MongoDB Atlas               │
│   (Backup/Fallback)   │  │    (Primary Storage)           │
│                       │  │                                │
│  • Browser-only       │  │  • Cloud-based                 │
│  • Instant writes     │  │  • Multi-device                │
│  • ~10 MB limit       │  │  • 512 MB free                 │
│  • Sync fallback      │  │  • Auto-backup                 │
│  • No auth needed     │  │  • Requires network            │
└───────────────────────┘  └────────────────────────────────┘
            │                        │
            │                        │
            │ If offline             │ If online
            │ Use localStorage       │ Sync to MongoDB
            │                        │
            └────────────┬───────────┘
                         │
                         ▼
                  ┌──────────────┐
                  │ User sees    │
                  │ latest data  │
                  └──────────────┘

Strategy:
1. Write to BOTH localStorage AND MongoDB
2. Read from MongoDB (if available)
3. Fallback to localStorage (if offline)
4. Merge conflicts: Last-write-wins
5. Show sync status to user
```

## 🎨 Component Structure

```
contest/
│
├── index.html
│   ├── Header
│   │   ├── Logo
│   │   ├── Streak Badge
│   │   ├── Sync Status ☁️
│   │   └── Action Buttons
│   │
│   ├── User Profile Section
│   │   ├── Handle Input
│   │   ├── Load Button
│   │   └── User Stats Display
│   │
│   ├── Contest Selection
│   │   ├── Division Cards (4)
│   │   ├── Custom Config
│   │   └── Quick Practice
│   │
│   ├── Contest Arena
│   │   ├── Timer Display
│   │   ├── Problems Table
│   │   ├── Submissions Panel
│   │   └── Control Buttons
│   │
│   ├── Results Section
│   │   ├── Score Summary
│   │   ├── Problem Results
│   │   └── Upsolve List
│   │
│   ├── History Section
│   │   ├── Filter Buttons
│   │   ├── Contest Cards
│   │   └── Performance Graph
│   │
│   └── Modals
│       ├── Settings
│       ├── Shortcuts
│       ├── Statistics
│       └── Confirmation
│
├── script.js
│   ├── State Management
│   ├── API Integration
│   ├── Codeforces API
│   ├── Contest Logic
│   ├── Timer System
│   ├── Sync Functions
│   └── UI Controllers
│
└── styles.css
    ├── Variables
    ├── Base Styles
    ├── Components
    ├── Themes (Light/Dark)
    └── Responsive
```

---

**This architecture supports:**
- ✅ Scalability to thousands of users
- ✅ Multi-device synchronization
- ✅ Offline-first capability
- ✅ Zero-downtime deployments
- ✅ Global CDN distribution
- ✅ Professional production setup

**Built for competitive programmers! 🚀**
